<mvc:View
    controllerName="com.mk.ui.mapping.controller.App"
    displayBlock="true"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc">

    <Shell appWidthLimited="false">
        <App
            id="app"
            busy="{appView>/busy}"
            busyIndicatorDelay="{appView>/delay}"/>
    </Shell>

</mvc:View>